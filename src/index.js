const swiper = new Swiper('.swiper', {
  // Optional parameters
  direction: 'horizontal',
  //   loop: true,
  //   autoplay: { delay: 5000 },

  // If we need pagination
  pagination: {
    clickable: true,
    el: '.testimonial-dots',
  },
});
